// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Canonical origin — used for absolute URLs in <head>, robots.txt and the sitemap.
  site: 'https://finant.ai',
});
